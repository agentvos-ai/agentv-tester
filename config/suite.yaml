active:
  framework: langchain
  llm: mock
  vertical: healthcare
agent:
  max_turns: 10
  temperature: 0.1
  verbose: true
server:
  endpoint: /execute_task
  host: 0.0.0.0
  port: 8080
shims:
  seed: 42
  stateful: true
